class User {
  final String uid;
  final String email;
  final String role; // Assuming role is a property you want to include

  User({
    required this.uid,
    required this.email,
    required this.role,
  });
}